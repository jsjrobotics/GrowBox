<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="number_row" parent="input_dialog_key">
    </style>

    <style name="number_pad" parent="input_dialog_key">
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="qwerty_key"  parent="input_dialog_key">
    </style>

    <style name="input_dialog_key" parent="android:Widget.Material.Light.Button.Borderless">
        <item name="android:minWidth">@dimen/min_key_size</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/dialog_text_size</item>
        <item name="android:padding">5dp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
